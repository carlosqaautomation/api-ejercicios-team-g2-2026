{
  "loginRequest": {
    "email": "#(user)",
    "password": "#(password)"
  },

  "accountCreationRequest":{
    "accountType": "#(type)",
    "currency": "#(currency)",
    "initialBalance": "#(balance)"
  }
}
